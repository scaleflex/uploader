import { TemplateResult } from 'lit';
/**
 * Crosshair "Locate" glyph — the inner shapes only, for insertion into an
 * existing `<svg viewBox="0 0 24 24">` whose stroke/size come from CSS (e.g.
 * the review-tile buttons in `file-item`).
 */
export declare const locateIconPaths: TemplateResult<2>;
/**
 * Crosshair "Locate" icon with the inline stroke styling the floating-pill and
 * success-card action buttons rely on. Size is left to the host's CSS
 * (`button svg { width: 14px; height: 14px }`).
 */
export declare const locateIcon: TemplateResult;
//# sourceMappingURL=icons.d.ts.map